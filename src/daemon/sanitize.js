"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class sanitize {
    provider(data) {
        const validData = {};
        let valid = () => {
            return true;
        };
        return valid ? validData : null;
    }
    mirror(data) {
        const validData = {};
        let valid = () => {
            return true;
        };
        return valid ? validData : null;
    }
    genericresolver(data) {
        const validData = {};
        let valid = () => {
            return true;
        };
        return valid ? validData : null;
    }
    streamresolver(data) {
        const validData = {};
        let valid = () => {
            return true;
        };
        return valid ? validData : null;
    }
}
exports.sanitize = sanitize;
