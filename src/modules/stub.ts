// Stub for vanillaJS requires, workaround for ide-typescript bug in Atom
const {register, genericResolver} = require("anv");
const request = require("request");
const {JSDOM} = require("jsdom");
const {jSh} = require("jshorts");
